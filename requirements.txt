# ── Runtime dependencies
httpx==0.27.0          # HTTP client (sync + async, modern API)
click==8.1.7           # CLI framework
rich==13.7.1           # Terminal formatting (tables, panels, colour)


── Optional / future
python-binance==1.0.19   # Official Binance SDK (if you prefer over raw REST)
websockets==12.0         # TODO: WebSocket support for live price feeds
sqlalchemy==2.0.30       # TODO: Persist trade history to PostgreSQL / SQLite
pandas==2.2.2            # TODO: Analytics on trade history
pytest==8.2.0            # TODO: Test suite
pytest-httpx==0.30.0     # TODO: Mock httpx for unit tests
